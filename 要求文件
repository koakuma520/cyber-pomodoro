# 赛博朋克桌面番茄钟 – 开发要求说明书

请根据以下要求，开发一款完整的“赛博朋克”风格桌面番茄钟应用。所有设计与功能必须符合下述四个维度的约束，确保代码质量与风格一致性。

---

## 1. 技术栈建议

请优先从下列选项中选择，并说明选择理由（可组合使用）：

- **方案 A：Electron + HTML/CSS/JS（原生）**  
  优点：可直接调用系统通知、托盘、开机自启等桌面能力，学习成本低。  
  缺点：打包体积较大。

- **方案 B：Electron + React + TypeScript + Vite**  
  优点：组件化开发，状态管理清晰，易于维护；Vite 构建快。  
  缺点：需要配置 Electron 主进程与渲染进程通信。

- **方案 C：Tauri + Web 前端（React/Vue）**  
  优点：打包体积小、内存占用低，使用 Rust 后端更安全。  
  缺点：对系统 API 的调用需额外实现，部分桌面特性可能需插件。

- **方案 D：纯 Web 单页应用（HTML/CSS/JS）+ PWA**  
  优点：无需安装，跨平台，易于部署。  
  缺点：桌面通知、后台运行等能力受限，不适合需要强桌面体验的场景。

**推荐**：若追求快速实现且需要桌面特性，采用 **方案 A**（Electron + 原生 HTML/CSS/JS） 或 **方案 B**（Electron + React）。若用户对体积敏感，可选 **方案 C**。构建时请务必使用 Canvas 实现粒子背景，并利用 CSS 动画处理扫描线、故障效果等，避免引入过重的图形库。

要求：无论选择哪种方案，最终软件必须能作为独立的桌面窗口运行，并支持以下特性：
- 系统托盘图标与菜单控制（显示/隐藏、退出）。
- 原生系统通知（Windows/macOS/Linux 均可）。
- 窗口可置顶，适合在工作时始终可见。
- 本地存储统计数据和设置（localStorage 或 electron-store 等）。

---

## 2. 核心功能

### 2.1 计时模式
- **番茄专注（Pomodoro）**：默认 25 分钟，可自定义时长。
- **短休息（Short Break）**：默认 5 分钟。
- **长休息（Long Break）**：默认 15 分钟。
- 三个模式可手动切换，每个模式有独立的默认时长；切换时计时器应重置。

### 2.2 计时器操作
- **启动/暂停**：点击开始专注计时，再次点击暂停（保留进度）；暂停时按钮文案变为“继续”。
- **重置**：一键恢复当前模式默认时长并停止计时。
- **自动切换**：番茄结束后自动进入短休息；每完成 4 个番茄后自动进入长休息（可配置）。自动切换时需给出显著视觉与通知提示。

### 2.3 进度可视化
- 环形进度条：随剩余时间减少，圆环从完整到消失。进度条颜色随模式变化（专注=青色，短休=品红，长休=亮绿）。
- 进度条末端有一个发光圆点，沿圆环运动，模拟“激光扫描”效果。
- 中央大数字显示剩余时间（MM:SS），字体使用等宽科技感字体。

### 2.4 通知与音频
- 计时结束时播放短暂的赛博朋克风格音效（可使用 Web Audio API 生成电子音，或提供默认音频文件）。
- 弹出系统通知，内容包含模式名称（如“番茄结束，休息一下吧”）。
- 屏幕轻微闪烁（白光/霓虹色叠加层），持续 0.3 秒，增强反馈。

### 2.5 统计与记录
- 展示当日完成的番茄总数、总专注分钟数。
- 使用 localStorage 或 JSON 文件持久化统计，每日重置或累计均可（需清晰标注）。
- 可选：点击统计区域展开历史记录列表。

### 2.6 设置与自定义
- 提供设置面板（齿轮图标），允许用户修改：
  - 番茄时长（1-60 分钟）
  - 短休息时长（1-30 分钟）
  - 长休息时长（1-60 分钟）
  - 长休息触发间隔（几个番茄后）
  - 通知音量或静音
- 设置立即生效，无需重启应用。

---

## 3. UI/UX 风格要求（赛博朋克）

### 3.1 色彩与主题
- 主背景：深蓝黑 `#06060F` 或 `#0A0A1C`，半透明磨砂面板。
- 主色调：**霓虹青 `#00F0FF`**（专注用）、**品红 `#FF00AA`**（休息用）、**荧光绿 `#00FF41`**（长休息用）、**电光黄 `#FFCC00`**（警告/运行状态）。
- 文字：浅灰白 `#C8D6E5`，次要文字降低透明度。
- 所有霓虹色元素必须有外发光效果（`text-shadow` / `box-shadow` 多层）。

### 3.2 视觉特效
- **粒子背景**：Canvas 全屏绘制缓慢移动的粒子（小圆点或六边形），粒子连线或随机闪烁，颜色以青色和品红为主。
- **扫描线**：半透明横向条纹自上而下缓慢移动，模拟 CRT 显示器效果。
- **噪点**：全屏 SVG/Canvas 噪点叠加，透明度极低。
- **故障艺术**：标题文字在计时结束或切换模式时触发一次轻微的位置偏移和色彩分离动画（clip-path 错位）。
- **发光圆点**：在进度环末端，随剩余时间移动，带有强烈的外发光。
- **按钮悬停/激活**：边框发光加强，背景霓虹色半透明覆盖，有微小的上浮效果（translateY）。

### 3.3 字体与排版
- 计时数字：使用 `'Orbitron'` 或类似科技感数字字体（可从 Google Fonts 引入或本地提供）。
- 正文和按钮：等宽字体 `'SF Mono', 'Cascadia Code', 'Consolas', 'Courier New'`，中文使用 `'Noto Sans SC'` 备用。
- 所有标签采用大写字母，字间距加宽。

### 3.4 布局与交互
- 窗口整体尺寸约 480px 宽，高度自适应，可缩放（最小宽度 340px）。
- 界面采用卡片式布局，居中显示，圆角边框带发光描边。
- 顶部状态栏显示系统时间（实时更新）和连接状态点（呼吸灯动画）。
- 模式选择器为胶囊按钮组，当前激活项边框和文字变为相应霓虹色。
- 主操作按钮为圆角矩形，使用当前模式颜色，暂停状态改为黄色强调。
- 鼠标光标在按钮上变为指针，在背景上为默认箭头。
- 界面需适配暗色模式下系统的色彩方案，始终保持赛博感。

### 3.5 微交互与动效
- 计时器数字变化时有轻微缩放（transform: scale(1.02) 再恢复）。
- 模式切换时，面板边框颜色和光晕平滑过渡（0.5s 缓动）。
- 通知 toast 从顶部滑入，带有弹簧动效。
- 粒子在鼠标靠近时轻微排斥或加速（可选增强沉浸感）。

---

## 4. 开发步骤约束

请严格按照以下阶段顺序开发，每个阶段完成后进行自测，确保后续步骤不会破坏已有功能：

### 阶段 1：项目初始化与静态界面
- 搭建 Electron（或所选框架）项目结构。
- 实现所有 HTML/CSS 布局，包括计时器圆环、按钮、模式选择器、统计面板、设置面板（默认隐藏）。
- 使用硬编码数据填充，确保静态界面的赛博朋克风格完全符合 UI 要求（粒子背景、扫描线、噪点、字体、配色）。
- 圆环进度可通过调整 `stroke-dashoffset` 手动测试。

### 阶段 2：计时器核心逻辑
- 实现番茄钟状态机（idle, running, paused, break 等状态）。
- 编写倒计时逻辑，使用 `setInterval` 或 `requestAnimationFrame`（精度到秒）。
- 将时间与圆环进度、中央数字绑定，计算偏移量并平滑过渡。
- 实现启动、暂停、重置功能，并正确切换按钮文案与状态。

### 阶段 3：模式切换与自动流转
- 完成三种模式的切换逻辑，切换时重置计时器至对应时长并更新 UI 颜色。
- 实现番茄完成后的自动休息逻辑，统计番茄计数。
- 处理 4 番茄后长休息的规则，以及用户手动干预时的计数重置（如手动跳过长休息）。
- 添加模式切换时的故障动画触发。

### 阶段 4：通知、音频与视觉反馈
- 集成 Web Audio API 或 Electron 原生音频播放，制作简短提示音（如电子哔声序列）。
- 实现系统通知（Electron 的 Notification 模块或浏览器 Notification API）。
- 添加计时结束时的屏幕闪烁效果。
- 顶部通知 toast 弹出，自动消失。

### 阶段 5：统计数据与持久化
- 将番茄完成次数、专注时长存储到本地。
- 更新统计面板显示。
- 每日重置逻辑：记录日期，跨天自动清零当日统计（历史可保留）。
- 若使用 Electron，考虑使用 `electron-store` 存储设置和统计。

### 阶段 6：设置面板与自定义
- 创建设置界面（可折叠侧边栏或弹窗），允许修改各时长和长休息间隔。
- 修改后立即反映到计时器默认值，并保存到本地存储。
- 音量控制或静音开关。

### 阶段 7：桌面集成特性
- 添加系统托盘图标（提供 16x16/32x32 的霓虹色图标），右键菜单包含“显示/隐藏”、“退出”。
- 窗口置顶选项（菜单或按钮切换）。
- 防止系统休眠（可选，专注模式下防止锁屏）。
- 打包为可执行文件，测试 Windows/macOS 兼容性。

### 阶段 8：优化与收尾
- 性能优化：降低粒子数量或使用 requestAnimationFrame 控制刷新率，避免 CPU 过高。
- 响应式适配：确保窗口缩放时布局不错乱，小窗口下自动缩小字体。
- 代码注释与文档：关键逻辑需注释，提供简单的运行说明。
- 添加应用图标（.ico / .icns）。

---

**请严格遵循上述所有要求进行开发，并在交付时提供完整的源代码和打包后的安装包（如果采用 Electron 或 Tauri）。**