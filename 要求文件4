一、文案与提示微调

1.“自适应”比例的含义补充
在“自适应”旁边加个小问号图标，悬浮提示“根据首帧图片自动选择最佳比例”，否则用户可能不清楚它在什么场景下生效。

2.“生成原生音频？”→“AI 生成环境音”
现在的表述稍显技术化，改成“自动匹配环境音效”或“AI 生成环境音”，旁边加小问号解释“模型根据画面自动生成背景声，非独立配音”。

3.历史记录引导文案可更有温度
目前的“生成你的第一个视频吧，它会出现在这里”已经很好，下方的“去生成”按钮可以改成“开始创作”。同时在这个区域中间加一个小图标（如 🎬 或 ✨），让视觉更亲切。

二、交互细节补完

1.“开始生成视频”按钮的状态流转
点击后建议按顺序变化：

正在提交... → 排队中（预计1-2分钟） → 生成中... → 完成！点击查看
至少要在按钮上展示一个加载动画（如旋转小圆圈）和“生成中...”文案，避免用户焦虑。

2.Ctrl+Enter 快捷键提示位置
现在这个提示放在输入框下方靠右，容易被忽略。建议移到输入框内部右下角，半透明显示，视觉上与输入框融为一体。

3.灵感墙点击后的视觉反馈
点击灵感卡片后，卡片应有短暂高亮或缩放效果（0.2秒），并且提示词自动填入后输入框也闪一下，让用户明确知道“已填入”。

4.高级参数的展开/收起动画
目前展开可能是瞬间的，加一个0.3秒的高度渐变过渡，体验会顺滑很多。

三、功能增强（打造记忆点）

1.视频生成完成后的“结果卡片”
生成完成后，不要只是在历史记录里多一条，建议在当前页面弹出一个半屏遮罩或卡片，展示：

视频播放器（自动静音播放）

“下载视频”按钮

“复制链接”按钮

“再来一条”按钮
做完这些操作后再自动存入历史记录。

2.“我的作品”独立页面
当历史记录超过10条，侧边栏会变得很长。建议历史记录面板只显示最近5条，底部加“查看全部 →”，点击后跳转到一个独立作品管理页，支持搜索和筛选。

